import React from "react";
import { useLinkClickHandler } from "react-router-dom";

// function displayTxInfo() {}
// everytime specific transaction is clicked, pass tx id as params to backend
// query ledger/couchdb to receive JSON data about specific transaction
// organize and display data throughout page
    // important data to display: tx id, amount, to/from, and timestamp
// }

function TxtData() {
    return (
        <div>
            <h1>TxtData!</h1>
        </div>
    );
}

export default TxtData;